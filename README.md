# SQL
SQL NOTES

## create table in sql
```
  create table lightningzero
  (
    full_name varchar(10),
    reg_no int,
    dob date,
    address varchar(50)
  );
```
<img src='img/create_table.png'>
